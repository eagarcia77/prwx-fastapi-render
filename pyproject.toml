[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prwx-hybrid-model"
version = "2.5.0"
description = "PR-WX v2.5 con PR-CARIBE WX Hybrid v2.0 para Puerto Rico y el Caribe"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.2",
  "numpy>=1.26",
  "requests>=2.31",
  "scikit-learn>=1.4",
  "joblib>=1.3",
  "pyyaml>=6.0",
  "python-dotenv>=1.0",
  "streamlit>=1.36",
  "plotly>=5.22",
  "matplotlib>=3.8",
]

[tool.setuptools.packages.find]
where = ["src"]
